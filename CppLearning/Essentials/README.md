# Essentials

## Vowels Counter
Today I implement a simple vowel counter wich works with a string type. 
I learned that **cin** doens't works so good if we use blank spaces so I instead used **getline** funtion to do it.